app.controller('EgonetGraph',function($scope,$compile,$http,$window,$timeout) {
    $scope.node = "VertexString"; // Use this for a vertex string
	//$scope.node = 123; // Use this for a vertex ID
});
